# No app-specific R8 rules currently needed. Kept as a hook for
# proguardFiles() in build.gradle.kts — see GitHub #57 for why the
# google_mlkit_text_recognition rules that used to live here are gone
# (swapped Tesseract in for F-Droid compatibility).
