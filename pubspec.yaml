name: xpenc
description: "XPENC — offline-first personal finance. Income, expense, transfers, budgets, persons and bank-message auto-capture, all on-device."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
#
# `AppInfo.version` / `AppInfo.buildNumber` mirror this line so the About screen
# can show it without pulling in a platform plugin. `test/branding_test.dart`
# parses this file and fails if the two ever disagree.
version: 1.5.1+17

environment:
  sdk: ^3.10.8

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: 2.6.1
  drift: 2.31.0
  drift_flutter: 0.2.8
  go_router: ^17.3.0
  intl: ^0.20.3
  fl_chart: ^1.2.0
  flutter_local_notifications: ^22.0.1
  timezone: ^0.11.1
  flutter_slidable: ^4.0.3
  sqlite3_flutter_libs: 0.5.24
  path_provider: ^2.1.6
  # Pinned below 13 so it shares win32 ^5 with file_picker (13 needs win32 ^6).
  share_plus: ^12.0.2
  # Import a backup file from anywhere on the device (transfer between phones).
  file_picker: ^8.1.6
  # Opens the developer's GitHub / LinkedIn from the About screen.
  url_launcher: ^6.3.2
  # Builds account/budget statement PDFs, entirely on-device — no network font
  # fetch, so statements stick to plain ASCII text (see statement_pdf.dart).
  pdf: ^3.11.3
  # Biometric unlock for the passcode lock screen (Settings > Security).
  local_auth: ^3.0.2
  # Hashes the passcode before it ever touches disk — never stored in plain
  # text (see core/security/passcode.dart).
  crypto: ^3.0.7
  # Bridges data to the Android home-screen widget (see android/…/widget) and
  # routes a tap on it back into the app.
  home_widget: ^0.9.3
  # Writes backups to the public Download/BACKUP XPENC folder via MediaStore —
  # no storage permission needed, and survives the app being uninstalled
  # (see backup_service.dart).
  media_store_plus: ^0.1.3
  # Lets a user Share a bank SMS from any app straight into XPENC — a
  # Play-compliant path into message capture, needing no READ_SMS permission
  # at all (see message_capture/share_intake.dart and GitHub #26).
  # Pinned below 1.9.0: that release bumped its own compileSdk to 37, an
  # Android preview-only platform build tooling can't reliably resolve.
  # 1.8.1's public API is byte-for-byte identical (compileSdk 34).
  receive_sharing_intent: 1.8.1
  # Parses a bank-exported statement for CSV import (see
  # data_export/csv_import_screen.dart and GitHub #17).
  csv: ^8.0.0
  # On-device text recognition for shared payment screenshots (PhonePe/GPay/
  # Paytm "payment successful" screens) — see
  # message_capture/ocr_service.dart and GitHub #25/#57. Tesseract (BSD-3),
  # not Google ML Kit: F-Droid rejects ML Kit's proprietary trained-model
  # blobs even in the "bundled" no-Play-Services form. Fully offline, no
  # extra Android permission — assets/tessdata/eng.traineddata ships in the
  # APK instead of being fetched at runtime.
  #
  # A local fork (packages/flutter_tesseract_ocr_floss), not the pub.dev
  # package: upstream vendors a prebuilt tesseract4android-release.aar
  # directly in its own android/libs/ — itself an unreviewable binary blob,
  # same class of problem ML Kit was (GitHub #57). The fork drops that file
  # and resolves the identical library from JitPack instead, which
  # F-Droid's Inclusion Policy explicitly allows. See that package's
  # android/build.gradle for the full rationale.
  flutter_tesseract_ocr:
    path: packages/flutter_tesseract_ocr_floss

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Lets tests fake out `path_provider` (see screenshot_intake_test.dart) —
  # already a transitive dependency of path_provider itself, declared
  # directly here only because tests import it by name.
  path_provider_platform_interface: ^2.1.3
  plugin_platform_interface: ^2.1.8

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: 2.31.0
  build_runner: ^2.15.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Tesseract's English trained-data model (Apache-2.0, tesseract-ocr/
  # tessdata_fast) — bundled at build time so OCR stays fully offline; see
  # message_capture/ocr_service.dart. tessdata_config.json is required by
  # flutter_tesseract_ocr itself (android_ios.dart's _loadTessData) — it's
  # the manifest the plugin reads to know which files to copy out of the
  # asset bundle into a real filesystem path before every OCR call; without
  # it declared as an asset, that copy step throws on every attempt.
  assets:
    - assets/tessdata/eng.traineddata
    - assets/tessdata_config.json

  # The Bold theme's own typography (see ThemeShape.bold) — bundled once,
  # offline, rather than fetched at runtime with a package like google_fonts.
  # Both are variable fonts: one file each covers every weight the app uses,
  # from the Sora headline at 800 down to Manrope's 400 body text.
  fonts:
    - family: Sora
      fonts:
        - asset: assets/fonts/Sora-Variable.ttf
    - family: Manrope
      fonts:
        - asset: assets/fonts/Manrope-Variable.ttf

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
